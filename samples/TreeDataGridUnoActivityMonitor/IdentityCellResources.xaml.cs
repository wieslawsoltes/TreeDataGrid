using Microsoft.UI.Xaml;

namespace TreeDataGridUnoActivityMonitor;

/// <summary>Owns the generated compiled-binding connector for identity cells.</summary>
public sealed partial class IdentityCellResources : ResourceDictionary
{
    public IdentityCellResources() => InitializeComponent();
}
